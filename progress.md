Original prompt: 我想开发一款类似4399平台中的黄金矿工小游戏

## 状态
- 可离线运行：`index.html` + classic `audio.js` + module `src/main.js`（导入 `game.js` browser host，Canvas）
- 商店与道具：炸药/加速/幸运袋（已实现）
- 关卡：固定关卡配置 + 随机种子（`?seed=12345`）
- 背景：内置 15 张 SVG 插画背景（每关按种子随机切换）
- 音效：WebAudio 合成 SFX（已实现）
- 背景音乐：新增 5 首舒缓纯音乐（WebAudio 合成，按关卡种子自动选曲，支持 N 换曲）
- 背景音乐：再新增 5 首舒缓纯音乐（共 10 首，按种子自动选曲）
- 钩索：最大伸出长度改为按画面对角线计算，侧边/角落物品可触达
- 角色：矿工重绘（更可爱、更细致），并加入“松手放绳/摇动绞盘回收”的动画（绞盘在前、矿工在后）
- 顶部结构：平板（木梁）下移，矿工与绞盘位于平板之上（并将绞盘绘制从平板拆分出来）
- 钩爪：完全重新设计为“三爪向内勾”造型（避免朝外翻）；未抓到时张开，抓到物体后夹住
- 钩爪：夹住时按物体半径自适配留缝（大物体更开，小物体更紧）
- 新增可抓取物品：金条/祖母绿/红宝石/水晶簇/古钱币袋/化石/炸药桶
- 炸药桶：抓到有 40% 立即爆炸；否则拉到半程松爪垂直坠落，碰到任何物体爆炸并清除范围内物品；未碰到则坠落出画面消失
- 经济：所有可抓取物品价值下调为原来的 40%（关卡目标/商店价格保持不变）
- 新增模式选择：进入游戏先选单人/双人；双人模式为双矿工双钩索（玩家1 空格/点击，玩家2 回车），过关目标分数 +30%
- 关卡物品数量：每关生成数量调整为原来的 70%
- 新“物品”：小老鼠（仅第 5 关后出现；水平折返移动；30% 概率背钻石/金条；每关最多 4 只）
- 强度：新增阶段性 + 自适应难度（根据本关得分相对目标的超额比例，动态调整下一关目标/时间/物品组成）
- 难度：抓到物体后的拉回时间 +30%（整体回收更慢）
- 经济（第4关起）：每关随机生成一个 50%~80% 的价值倍率，作用于该关所有物品价值（四舍五入）
- 行情系统：每关开局生成“当日行情”，金条/钻石/祖母绿/红宝石/水晶簇价格独立波动并展示在顶部行情栏
- 难度：第5关起启用递增压力曲线（目标倍率上调、时间倍率下调、重负担物品占比上升、高价值物品占比下降、老鼠速度提高）
- 平衡：每关物品刷新密度从 70% 下调到 55%，关卡最低时间统一放宽到 45 秒
- 音效：放钩、空钩回收、带物回收拆分为不同钩索音效；抓到不同物品时采用不同拟真音色
- 音效：绞盘持续转动音轨升级为“工业重齿轮”参数（更低频扭矩层 + 更密集棘轮咔嗒）
- 音效：绞盘音色进一步调亮为更清脆机械风（提高齿轮/棘轮高频，降低低频闷感）
- 音效：绞盘“金属脆响 + 低频下探”版（新增高频 clink 层，同时下调主体基频）
- 音效：绞盘“极清脆”版（新增 clink 脉冲层，进一步降低低频底噪占比）
- 音效：已移除绞盘持续转动声音
- 测试钩子：增加 `window.render_game_to_text` 与 `window.advanceTime(ms)`（用于 Playwright 自动化）

## 启动/运行
- macOS 双击：`run.command`（启动本地 `http.server`，打开 `http://127.0.0.1:5173/`；若环境禁止打开 GUI，会输出该 HTTP URL 供手动复制打开）
- Windows 双击：`start.bat`（启动本地 `http.server`，打开 `http://127.0.0.1:5173/`）
- Linux/macOS 终端：`./start.sh`（启动本地 `http.server`，打开或输出 `http://127.0.0.1:5173/`）
- 说明：当前入口使用 ES module `src/main.js`，推荐通过本地静态服务器运行，不再依赖直接 `file://` 打开 `index.html`。

## 已知限制（受限执行环境）
- 若环境禁止绑定端口，本地服务器无法启动；可在允许绑定本机端口的终端运行 `python3 -m http.server 5173 --bind 127.0.0.1`。
- 若环境禁止启动 GUI 应用（`open`/浏览器可能失败），启动脚本会打印 `http://127.0.0.1:5173/` 供手动复制打开。

## TODO（下一步可选）
- 把 BGM 做成可在 UI 中切歌/显示列表（当前仅快捷键 N）
- 如需自动化截图测试：安装 Node + Playwright 后运行 `web_game_playwright_client.js`
- 架构 Batch 3：关卡配置、物品规格、价值计算和关卡生成已抽离为纯模块；运行时通过 `GoldMinerModules.generateLevelData()` 接入，classic entry 保持不变。
- 架构 Batch 4：已建立 command vocabulary、state selectors、inventory helpers 和 pure command dispatcher；主要 UI/键盘/pointer/shop 入口通过 `dispatchCommand()` 进入现有 runtime handlers。下一批建议做 Event Bus For Side Effects。
- 架构 Batch 5：已建立 runtime event queue、事件类型/队列、audio/ui/fx adapters 和 bridge fallback helper；选定 audio、HUD、overlay、shop、FX、炸弹、抓取、得分、倒计时副作用已通过事件 helper 路由。下一批建议做 Render Layer Split。
- 架构 Batch 6：已建立 `src/render/renderPipeline.js`，`game.js render()` 通过 bridge render pipeline 编排清屏、DPR/shake transform、玩家排序、图层顺序、flash/countdown overlay，并保留本地 fallback。具体 `draw*` 函数仍在 `game.js`；下一步可继续拆具体 renderer，或进入 UI/Input/Audio/Test Harness Cleanup。
- 架构 Batch 6 follow-up：新增 `src/render/backgroundRenderer.js`，`drawBackground()` / `drawPlank()` 现在优先通过 bridge renderer 绘制背景与木板，异常时一次性熔断并回退本地绘制。剩余 concrete renderer：item、hook、miner、winch、carry label、FX。
- 架构 Batch 6 follow-up：新增 `src/render/itemLayerRenderer.js`，`drawItems()` 现在优先通过 bridge item layer 处理物品排序、被抓物后置和 duplicate attached id 去重，异常时一次性熔断并回退本地排序。具体 `drawItem()` 几何仍在 `game.js`；下一步可拆 `hookRenderer` 或继续细分 `drawItem()`。
- 架构 Batch 6 item layer 评审修正：bridge 与本地 fallback 统一为正整数 `attachedId` 判定，本地 fallback 也按 `drawItem(item, metadata)` 契约调用；最小验证集 `node --test tests/unit/item-layer-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 26/26。
- 架构 Batch 6 item layer 最终验收：`npm run verify` 通过 88/88，`./macos/build.command` 成功生成 app；Browser 烟测进入单人局、触发出钩并得分，控制台无 error/warn，截图见 `output/gold-miner-item-layer-final.png`。
- 架构 Batch 6 follow-up：新增 `src/render/hookLayerRenderer.js`，`renderLayerHandlers()` 现在通过 bridge-first hook layer handlers 统一分发 `hookTrail` / `hook` / `carryLabel`，异常时一次性熔断并回退本地 callbacks。具体 `drawHookTrail()` / `drawHook()` / `drawCarryLabel()` 几何仍在 `game.js`。
- 架构 Batch 6 hook layer 最终验收：spec reviewer 与 quality reviewer 均 PASS；`npm run verify` 通过 93/93，`./macos/build.command` 成功生成 app；Playwright 烟测确认 bridge module ready、`createHookLayerHandlers` 存在、无 hook renderer error、canvas 非空、控制台无 error/warn，canvas 截图见 `output/gold-miner-hook-layer-final-canvas.png`。
- 架构 Batch 6 follow-up：新增 `src/render/hookTrailRenderer.js`，`drawHookTrail()` 现在优先通过 bridge renderer 绘制钩索轨迹，异常时一次性熔断并回退本地绘制。hook trail 点生成、attached item 颜色选择以及 `drawHook()` / `drawCarryLabel()` 仍在 `game.js`。
- 架构 Batch 6 hook trail 最终验收：spec reviewer PASS；quality reviewer 发现并已修复本地 fallback 未使用 `options.ctx` 且缺少 `try/finally` restore 的问题，复核 PASS。`npm run verify` 通过 98/98，`./macos/build.command` 成功生成 app；Playwright 烟测确认 bridge module ready、`drawHookTrailLayer` 存在、无 hook trail renderer error、canvas 非空、控制台无 error/warn，canvas 截图见 `output/gold-miner-hook-trail-final-canvas.png`。
- 架构 Task 1 carry label renderer：新增 `src/render/carryLabelRenderer.js` 并通过 `GoldMinerModules.drawCarryLabelLayer()` 接入 `drawCarryLabel()` bridge-first wrapper；状态判断保留在 `carryLabelLayerOptions()`，本地 fallback 使用 `options.ctx` 和 `try/finally` restore。验证：`node --test tests/unit/carry-label-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 26/26，`npm run verify` 通过 103/103。
- 架构 Task 1 carry label renderer 最终验收：`./macos/build.command` 成功生成 app；Playwright MCP 烟测确认 `window.__goldMinerModulesReady === true`、`GoldMinerModules.drawCarryLabelLayer` 存在、无 `__goldMinerCarryLabelRendererError` 或其他 renderer error、canvas 非空、控制台 0 error/warn，canvas 截图见 `output/gold-miner-carry-label-final-canvas.png`。Spec reviewer PASS；quality reviewer PASS，仅指出 `game.js` bridge 失败路径仍缺少可执行宿主级单测，该项已纳入后续 Test Harness/Regression Hardening 批次处理。
- 架构 Task 2 winch/reel renderer：新增 `src/render/winchRenderer.js`，导出 `drawReelLayer()` 与 `drawWinchLayer()` 并通过 `GoldMinerModules.drawWinchLayer()` 接入 `drawWinch()` bridge-first wrapper；`winchLayerOptions()` 汇总 ctx/pivot/reel/plankY/hook，本地 fallback 使用 `options.ctx` 和 `try/finally` restore，bridge 失败记录 `__goldMinerWinchRendererError` 后一次性熔断。验证：`node --test tests/unit/winch-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 与 `npm run verify` 已按 Task 2 执行。
- 架构 Task 2 自动审查修正：quality reviewer 发现 bridge `roundRectPath()` 与本地 fallback 的 `roundRectPathForWinch()` 路径语义不一致（`lineTo/arc` vs `arcTo/closePath`）；已修复为同构 `arcTo/closePath`，并补充 renderer/source invariant 断言防止回归。复核 PASS；轻量目标测试 30/30 通过。为降低机器负载，Task 2 的 macOS build 与浏览器冒烟延后到本组渲染抽离统一执行。
- 架构 Task 3 miner renderer：新增 `src/render/minerRenderer.js`，导出 `createMinerPose()`、`drawMinerBackLayer()`、`drawMinerFrontLayer()` 并通过 `GoldMinerModules` 接入 `drawMinerBack()` / `drawMinerFront()` bridge-first wrappers；`minerLayerOptions()` 汇总 ctx/hook/miner/pivot/reel/now/attachedItem，本地 fallback 使用 `options.ctx` 和 `try/finally` restore，bridge 失败记录 `__goldMinerMinerRendererError` 后一次性熔断。验证：`node --test tests/unit/miner-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 29/29，`npm run verify` 通过 117/117。按本轮低负载指令未执行 macOS build、浏览器烟测或启动 server。
- 架构 Task 3 自动审查修正：quality reviewer 发现 `drawMinerBackWithLocalLayer()` / `drawMinerFrontWithLocalLayer()` 本地 fallback 中多处内层 `ctx.save()` 缺少独立 `try/finally`，bridge 失败路径可能泄漏 canvas state；已为每个内层 save 补齐 finally restore，并在 source invariant 中增加 save/finally restore 计数一致断言。复核 PASS；目标测试 29/29 通过，`npm run verify` 通过 117/117。
- 架构 Task 4 hook rope/claw renderer：新增 `src/render/hookRenderer.js`，导出 `drawHookLayer()` 并通过 `GoldMinerModules.drawHookLayer()` 接入 `drawHook()` bridge-first wrapper；`hookShapeLayerOptions()` 汇总 ctx/hook/pivot/tip/dir/carriedItem/canBomb/HOOK/now/itemGlowColor，原 hook layer callbacks 仍继续传 `drawHook`。本地 fallback 使用 `options.ctx`，并为每个 `ctx.save()` 补齐独立 `try/finally` restore；bridge 失败记录 `__goldMinerHookShapeRendererError` 后一次性熔断。验证：`node --test tests/unit/hook-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 32/32，`npm run verify` 通过 125/125。按本轮低负载指令未执行 macOS build、浏览器烟测或启动 server。
- 架构 Task 4 自动审查修正：quality reviewer PASS，仅指出测试偏紧耦合和审计记录粒度；本地复核时补强 `drawHookLayer()` 输入契约，要求 retract carried item 必须提供 `itemGlowColor`，避免 `CanvasGradient.addColorStop()` 接收空颜色。复跑 `node --test tests/unit/hook-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 32/32。
- 架构 Task 5 item shape renderer：新增 `src/render/itemRenderer.js`，将 `blobPath()` 与 concrete `drawItem()` 几何抽成纯 `drawItemShape({ ctx, item, metadata, now, createRng })` 的 bridge renderer，同时在 `game.js` 保留独立本地 fallback 镜像；`src/runtime/moduleBridge.js` 暴露 `GoldMinerModules.drawItemShape()`，`game.js drawItem(item, metadata)` 改为 bridge-first wrapper，新增 `itemShapeLayerOptions()` / `drawItemWithLocalLayer()`，bridge 失败记录 `__goldMinerItemShapeRendererError` 并一次性熔断。本地 fallback 与模块版都改为 save/restore helper 包裹，避免 canvas state 泄漏；fallback blob art 继续用注入的 `createRng(item.id)`。验证：`node --test tests/unit/item-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 31/31。按本轮低负载指令未执行 macOS build、浏览器烟测、local HTTP server 或截图。
- 架构 Task 5 自动审查修正：quality reviewer PASS，仅指出已有 `art.blob` 时不调用 fallback rng 的回归保护略薄，以及审计表述需明确 fallback 镜像并存；已补充 `drawItemShape uses supplied gold blob art without consulting fallback rng` 单测，并修正 Task 5 进度表述。
- 架构 Task 6 FX renderer：新增 `src/render/fxRenderer.js`，将 shockwave rings、particles、score pops 抽成纯 `drawFxLayer({ ctx, fx })` bridge renderer，并在 `src/runtime/moduleBridge.js` 暴露 `GoldMinerModules.drawFxLayer()`；`game.js drawFx()` 改为 bridge-first wrapper，新增 `fxLayerOptions()`、`withSavedFx()`、`drawFxWithLocalLayer()`，bridge 失败记录 `__goldMinerFxRendererError` 并一次性熔断。本地 fallback 继续保留运行时状态组装，且所有 canvas save/restore 均通过异常安全 helper 包裹；renderer 与测试都约束 `fx`/数组/条目不被修改。验证：`node --test tests/unit/fx-renderer.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 31/31。按本任务指令未执行 `./macos/build.command`、浏览器烟测、local HTTP server 或截图，留待 render-group checkpoint。
- 架构 Task 6 自动审查修正：quality reviewer PASS，仅指出测试计数记录滞后、bridge 失败路径仍主要依赖 source invariant、浮点 alpha 精确断言略脆；已修正测试计数，bridge 失败路径纳入后续 Test Harness/Regression Hardening 批次统一处理。
- 架构 render extraction group checkpoint：Task 1-6 阶段性重验证完成。`npm run verify` 通过 138/138；`./macos/build.command` 成功生成 `/Users/drew/Project/Gold Miner/dist/macos/黄金矿工.app`；浏览器 smoke 进入 seed 12345 单人局并触发出钩，确认 `window.__goldMinerModulesReady === true`、`drawCarryLabelLayer` / `drawWinchLayer` / `createMinerPose` / `drawMinerBackLayer` / `drawMinerFrontLayer` / `drawHookLayer` / `drawItemShape` / `drawFxLayer` 均为 function，无 `__goldMiner*RendererError`，控制台 0 error/warn，canvas 采样非空（76800/76800），截图见 `output/gold-miner-renderer-group-canvas.png`。
- 架构 Task 7 scene/render snapshot boundary：新增 `src/render/sceneSystem.js` 与 `src/render/renderSnapshot.js`，分别导出纯 `createSceneData({ seed, viewport, background })` 与 `createRenderSnapshot({ game, ctx, canvas, dpr, players, layers, now })`；`src/runtime/moduleBridge.js` 暴露这两个边界；`game.js buildScene()` 与 `render()` 改为 bridge-first，并分别在 scene system / render snapshot 失败时记录 `__goldMinerSceneSystemError` / `__goldMinerRenderSnapshotError` 后一次性熔断回退本地逻辑。验证：`node --test tests/unit/scene-system.test.mjs tests/unit/render-snapshot.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 34/34，`npm run verify` 留待本任务收尾执行；按任务指令未执行 `./macos/build.command`、浏览器 smoke、local HTTP server 或截图。
- 架构 Task 7 自动审查修正：本地复核补强 `hasGoldMinerModuleInterface()`，将 `createSceneData` 与 `createRenderSnapshot` 纳入基础 bridge interface 兼容判断，避免旧 namespace 被误判 ready 后只靠局部 fallback；目标测试仍通过 34/34。quality reviewer PASS，仅指出宿主级熔断路径仍主要依赖 source invariant，该项继续纳入 Test Harness/Regression Hardening 批次统一处理。
- 架构 Task 8 FX state update system：新增 `src/systems/fxSystem.js`，导出原地 mutate 并返回同一对象的 `updateFxState(fx, dt, random = Math.random)`；`src/runtime/moduleBridge.js` 暴露 `GoldMinerModules.updateFxState()`；`game.js updateFx(dt)` 改为 bridge-first wrapper，失败时记录 `__goldMinerFxSystemError` 后一次性熔断并回退 `updateFxWithLocalState(dt)`。新增 `tests/unit/fx-system.test.mjs` 覆盖 flash/shake、random 注入、pops/rings/particles 推进删除、默认 gravity 和基本输入校验，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/fx-system.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 34/34。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、并行或长期任务。
- 架构 Task 8 自动审查修正：spec reviewer 发现 `src/runtime/moduleBridge.js` 仅将 `updateFxState` 挂到 `GoldMinerModules`，缺少模块级命名导出；已补 `export { updateFxState }`，并在 runtime bridge/source invariant 中加入断言。quality reviewer PASS；已补充“shake 本帧衰减到 0 仍调用两次 random”的兼容性单测，宿主级熔断行为测试继续留到 Test Harness/Regression Hardening 批次统一处理。
- 架构 Task 9 hook state system first extraction：新增 `src/systems/hookSystem.js`，抽出纯 `getHookDir()`、`getHookEndPoint()`、`clampHookLength()`、`updateHookTrailState()`，并额外抽出 `updateHookSwingState()` / `updateHookReelState()`；`game.js` 对 hook dir/end/trail/swing/reel/resize length clamp 改为 bridge-first wrapper，失败时记录 `__goldMinerHookSystemError` 后一次性熔断并回退本地镜像逻辑，未移动碰撞、attach、delivery、dust puff 或 audio/event 副作用。`src/runtime/moduleBridge.js` 增加模块级命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/hook-system.test.mjs` 覆盖方向、终点、长度 clamp、trail aging/removal、active add/update/shift、inactive 不添加、swing/reel 数值推进和输入校验，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/hook-system.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 41/41。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、并行或长期任务。
- 架构 Task 9 自动审查修正：spec reviewer PASS；quality reviewer PASS，并指出 `updateHookReelState()` 对旧 `lastLength` 的校验比实际计算需要更严格。已移除该无必要校验，补充 trail 删除后 append、7px 阈值、非 swing 状态、负 delta、近零 delta 和缺省 `lastLength` 的边界测试。目标验证更新为 `node --test tests/unit/hook-system.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 45/45；宿主级熔断行为测试继续留到 Test Harness/Regression Hardening 批次统一处理。
- 架构 Task 10 item motion and keg system boundary：新增 `src/systems/itemMotionSystem.js`，抽出 mouse 横向移动/边界折返/Y clamp/phase 推进与 falling keg 锁定 X、重力积分和 art rot 更新；新增 `src/systems/kegSystem.js`，抽出 falling keg collision target selection 与 keg blast affected ids 选择，两个模块均只做纯状态/数据计算，不访问 `game/window/ctx/DOM/audio/FX`。`game.js` 的 `updateMice()`、`updateFallingKegs()` 和 `explodeKegAt()` 改为 bridge-first 调用，失败分别记录 `__goldMinerItemMotionSystemError` / `__goldMinerKegSystemError` 后熔断到本地 fallback；爆炸 FX/audio、`game.items` 删除、hook attached 清理、`dropKeg()` FX/audio 和 `attachToHook()` 的 `Math.random()` 路径均保留在宿主。`src/runtime/moduleBridge.js` 增加模块级命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/item-motion-system.test.mjs` 与 `tests/unit/keg-system.test.mjs`，补强 runtime bridge/source invariant。验证：`node --test tests/unit/item-motion-system.test.mjs tests/unit/keg-system.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 46/46。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、并行或长期任务。
- 架构 Task 10 自动审查：spec reviewer PASS；quality reviewer PASS，确认 mouse/keg motion、碰撞阈值、blast 半径公式与本地 fallback 对齐，且模块未触碰 FX/audio/DOM 等副作用。非阻塞风险仍是宿主级 bridge failure fallback 主要由 source invariant 约束，继续纳入 Test Harness/Regression Hardening 批次统一补行为测试。
- 架构 Task 11 scoring and delivery system boundary：新增 `src/systems/scoringSystem.js`，导出纯 `createDeliveryResult({ score, item, playerIndex })`，只计算 earned/nextScore/color 与 score pop/audio/ring/burst payload，不访问 `game/window/ctx/DOM/audio/FX` 且不修改 item；`game.js deliverAttachedItem()` 改为 bridge-first 调用 `GoldMinerModules.createDeliveryResult()`，失败记录 `__goldMinerScoringSystemError` 后一次性熔断到本地同构 fallback，宿主继续负责写 `game.score`、记录 first clear time、删除 delivered item、发 audio/score pop/ring/burst 事件并 drain events。`src/runtime/moduleBridge.js` 增加模块级命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/scoring-system.test.mjs` 覆盖 bag value fallback、非 bag value、nextScore、playerIndex、颜色分支、burst clamp、immutability 和输入校验，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/scoring-system.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 40/40。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、并行或长期任务。
- 架构 Task 11 自动审查修正：spec reviewer PASS；quality reviewer PASS，确认 scoring module 未接管宿主副作用且 delivery payload 与旧逻辑等价。已将 scoring bridge 错误记录改为只写首次 `__goldMinerScoringSystemError`，与其他系统熔断模式保持一致，并补强 source invariant；宿主级 bridge failure 行为测试继续留到 Test Harness/Regression Hardening 批次统一处理。
- 架构 Task 12 gameplay RNG boundary：新增 `src/core/randomStreams.js`，通过 `createRandomStream({ runSeed, levelSeed, name, salt })` 从 run seed、关卡 seed、命名流和 salt 派生 deterministic stream，并在 `src/runtime/moduleBridge.js` 增加命名导出与 `GoldMinerModules.createRandomStream()`。`game.js` 新增每关重置的 `game.randomStreams.kegImmediate`，`attachToHook()` 的炸药桶 40% 立即爆炸判断改为 `shouldKegExplodeImmediately()`，bridge 失败记录 `__goldMinerRandomStreamsError` 后回退本地同构 helper；`spawnBurst`、`addScorePop`、FX shake jitter 和 `updateFxState(game.fx, dt, Math.random)` 保持视觉随机。新增 `tests/unit/random-streams.test.mjs` 并补强 runtime bridge/source invariant。按低负载指令仅运行目标测试，未执行 macOS build、浏览器 smoke、local HTTP server、并行或长期任务。
- 架构 Task 12 自动审查：spec reviewer PASS；quality reviewer PASS，确认 stream 按 runSeed/currentSeed/name/salt 确定性派生、每关重置且不会每次判断重新创建；`attachToHook()` 不再调用 `Math.random()`，视觉随机保留在允许范围内。非阻塞建议是后续在 Test Harness/Regression Hardening 批次补固定 seed 下多次 keg 判断与跨关重置的宿主行为测试。
- 架构 Task 13 UI adapter boundary：新增 `src/ui/domUiAdapter.js`，导出纯 `createHudSnapshot({ game, canBomb })` 负责 HUD 文案、按钮 disabled、danger class 和行情 fallback 格式化，导出 `applyHudSnapshot({ ui, uiRefs, snapshot, previous, bump })` 负责 DOM 写入、class toggle、inventory/score bump 与 `hudPrev` mutation；adapter 不访问 `window` / `document` / hook gameplay state。`game.js updateHud()` 现在仍在宿主侧计算依赖 `getHooks()` / `attachedItem()` 的 `canBomb`，随后 bridge-first 调用 `GoldMinerModules.createHudSnapshot()` / `applyHudSnapshot()`，失败记录 `__goldMinerUiAdapterError` 后熔断到本地同构 DOM fallback。`src/runtime/moduleBridge.js` 增加命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/dom-ui-adapter.test.mjs`，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/dom-ui-adapter.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 39/39。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、全量 verify、并行或长期任务。
- 架构 Task 13 自动审查：spec reviewer PASS；quality reviewer PASS，确认 HUD 文案/取整、按钮 disabled、danger class、bump 条件、`hudPrev` mutation 与旧逻辑一致，adapter 未扩大到 overlay/shop/audio buttons。非阻塞建议是后续在 Test Harness/Regression Hardening 批次补 UI adapter bridge failure 的宿主行为测试。
- 架构 Task 13 自动审查：Subagent code reviewer `PASS_WITH_CONCERNS`，未发现 Task 13 规格或质量问题；concern 仅为子代理按低负载约束未执行测试。主线 targeted tests 已执行并通过，真实 DOM/宿主级熔断行为仍主要由 source invariant 与 adapter fake DOM 单测覆盖，后续可在 Test Harness/Regression Hardening 批次补宿主行为测试。
- 架构 Task 14 input adapter boundary：新增 `src/ui/inputAdapter.js`，将 button / keyboard / pointer 输入映射抽成纯 command descriptor adapter，不访问 `game/window/document/canvas/DOM`、不 dispatch、不修改传入 state；`game.js initUi()` 继续注册真实 listeners，但 button / keydown / pointerdown 的 command selection 现在经 `map*InputForHost()` bridge-first wrapper，失败记录 `__goldMinerInputAdapterError` 后熔断到本地同构 fallback，listener 仍按 adapter 返回的 `preventDefault` 调用 `e.preventDefault()`。`src/runtime/moduleBridge.js` 增加 `mapButtonInput` / `mapKeyboardInput` / `mapPointerInput` 命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/input-adapter.test.mjs` 覆盖旧按键、按钮、pointer 映射、preventDefault、no-command 和输入校验，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/input-adapter.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 45/45。按低负载指令未执行 macOS build、浏览器 smoke、local HTTP server、全量 verify、并行或长期任务。
- 架构 Task 14 自动审查：子代理自审 `DONE_WITH_CONCERNS`，主线复核未发现规格或质量阻塞；input adapter 保持纯映射，宿主侧副作用仍集中在 `game.js` listener 与 command dispatch。非阻塞 concern 仍是 bridge failure 的真实宿主行为主要由 source invariant 覆盖，继续纳入 Test Harness/Regression Hardening 批次统一补行为测试。目标测试复跑通过 45/45。
- 架构 Task 15 audio adapter boundary：新增 `src/audio/audioAdapter.js`，导出 `createAudioButtonSnapshot()` 与 `applyAudioEventsToFacade()`，将音频按钮状态读取和 `AUDIO_PLAY` / `AUDIO_SYNC_BUTTONS` 到 `window.GameAudio` facade 的调用路径抽成可测试 adapter，保留 `audio.js` WebAudio 合成逻辑不变。`game.js` 的 `syncAudioButtons()`、`renderShop()` 音乐名读取与 `applyRuntimeAudioEvents()` 现在优先通过 `GoldMinerModules` audio adapter，失败记录首个 `__goldMinerAudioAdapterError` 并回退本地同构路径；`src/runtime/moduleBridge.js` 增加命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/audio-adapter.test.mjs`，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/audio-adapter.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 42/42。
- 架构 Task 15 自动审查：code reviewer PASS，复核确认 audio adapter 不访问 host globals、`applyRuntimeAudioEvents()` 保持 bridge-first/fallback-second、AUDIO_PLAY 仍先 init 后 play，AUDIO_SYNC_BUTTONS 仍触发按钮同步，非 audio events 忽略。审查后额外将 `renderShop()` 的音乐名读取收口到 `createAudioButtonSnapshotForHost()`，复核 PASS。非阻塞 concern 仍是 bridge failure 真实宿主行为测试后续在 Test Harness/Regression Hardening 批次统一补。
- 架构 Task 16 test harness / debug API boundary：新增 `src/testing/debugApi.js`，导出 `createDebugSnapshot()`、`renderDebugSnapshotToText()` 和 `createDebugAdvancePlan()`，将 `window.render_game_to_text()` 的 JSON 快照组装、rounding、market/DDA/hook/items 形状和 `window.advanceTime(ms)` 的 60fps 步数/dt 计算抽成纯测试边界。`game.js` 现在只负责构造 `debugSnapshotInput()`、调用 bridge/debug fallback、记录首个 `__goldMinerDebugApiError`、执行 `stepFrame(plan.dt)` 与 `render()`；为避免宿主侧重复实现，`loadGoldMinerModules()` 缓存 bridge/debug module exports 到 `debugApiFallback`，moduleBridge 失败时单独 dynamic import `./src/testing/debugApi.js` 作为 debug fallback。新增 `tests/unit/debug-api.test.mjs`，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/debug-api.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 42/42。
- 架构 Task 16 自动审查修正：code reviewer 初审 FAIL，指出 `game.js` 中保留完整 debug snapshot 和 advance plan 副本会违背边界目标并造成漂移；已删除 `createDebugSnapshotWithLocalData`、`createDebugAdvancePlanWithLocalData` 等宿主侧重复实现，改为复用 `src/testing/debugApi.js` 的同一份 pure helper 作为 fallback。复核 PASS，剩余低风险为预置兼容 `window.GoldMinerModules` 的 fallback 独立性依赖其实现等价。
- 架构 Task 17 state kernel assembly：新增 `src/state/createInitialState.js`，通过 `createInitialGameState({ minRope, viewport })` 承载默认 runtime state shape，保留原有 phase/mode/market/items/scene/inventory/effects/events/audio/dda/fx/hook/hook2/viewport/miner/miner2 默认字段，并确保 nested arrays/objects 是 fresh 实例。新增 `src/state/stateKernel.js`，通过 `stepPlayingState({ state, dt, systems, events })` 承载 playing frame shell：非 playing 直接停止、扣减 `timeLeft`、time-up 归零并调用 `events.endLevel()`、10 秒倒计时 bucket 变更时更新 `state.audio.lastCountdownSec` 并调用 `events.countdown(sec)`。`game.js` 在 boot 加载 bridge 后通过 `initializeGameState()` 初始化 `game`，`update(dt)` 顶部改为 `stepPlayingStateForHost(dt)`，宿主侧保留 countdown 音频事件、`endLevel()` 和后续物理/UI glue；`src/runtime/moduleBridge.js` 增加命名导出并挂载到 `GoldMinerModules`。新增 `tests/unit/state-kernel.test.mjs`，并补强 runtime bridge/source invariant。验证：`node --test tests/unit/state-kernel.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 46/46。
- 架构 Task 17 自动审查：code reviewer PASS，确认 state factory 保留默认 shape，state kernel 不访问 DOM/Canvas/Audio，`game.js` 符合 bridge-first/fallback-second 初始化和 playing time gate。剩余非阻塞风险：state-kernel fallback 缺少模拟 bridge 抛错后的宿主集成行为测试，完整 state 字段不是全量 snapshot 断言。
- 架构 Task 18 bridge surface cleanup：保守收紧 runtime bridge 公开面，移除已由 `src/audio/audioAdapter.js` / `GoldMinerModules.applyAudioEventsToFacade()` 取代的 `GoldMinerModules.applyAudioEvents`，保留 `src/audio/audioEvents.js` 内部模块和单测使用不变；未删除尚未重新做浏览器 smoke 的宿主 fallback。`tests/unit/runtime-bridge.test.mjs` 新增 `EXPECTED_GOLD_MINER_MODULE_KEYS`，对 `Object.keys(GoldMinerModules)` 做精确白名单断言，并断言 deprecated `applyAudioEvents` 不再公开；`tests/unit/source-invariants.test.mjs` 增强解析 helper，禁止 `moduleBridge` 从 `../audio/audioEvents.js` 直接导入 `applyAudioEvents`（含 alias import）、禁止命名导出和 `GoldMinerModules` object literal 中重新挂载该符号。验证：`node --test tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs` 通过 38/38。
- 架构 Task 18 自动审查修正：code reviewer 两轮 FAIL 都集中在 source invariant 防回归断言过窄，已改为 import specifier 解析、alias 归一化和 object literal 检查；第三轮复核 PASS。剩余非阻塞风险：source invariant 仍基于源码文本，未来若引入非常规 import 书写方式需要同步增强解析。
- 架构 Task 19 module entrypoint migration：新增 `src/main.js`，先安装 `src/runtime/moduleBridge.js`，再 dynamic import `../game.js` 作为现有 browser host；`index.html` 改为先加载 classic `audio.js`，再加载 `<script type="module" src="./src/main.js"></script>`，不再直接加载 `game.js`。保留 `game.js` 自身 dynamic bridge fallback 以支持直接 host 调试和兼容路径；`macos/build.command` 已复制整个 `src/`，无需额外改动。新增 `tests/unit/main-entry.test.mjs`，并重写 source invariant 中的入口断言；架构文档同步记录 Batch 7 后的当前入口。验证：`node --check src/main.js && node --test tests/unit/main-entry.test.mjs tests/unit/source-invariants.test.mjs tests/unit/runtime-bridge.test.mjs` 通过 42/42；code reviewer PASS。按低负载策略，macOS build 与浏览器 smoke 留到最终整体验收。
- 架构 Task 19 全量 Node 验证：`npm run verify` 通过 242/242；未启动浏览器、local server 或 macOS build，继续留到最终整体验收。
- 架构 Task 20 type/regression hardening：`package.json` 的 `check:syntax` 扩展为检查 `game.js`、`audio.js`、`src/**/*.js` 和 `tests/unit/**/*.mjs`；新增 `tests/unit/dependency-boundaries.test.mjs`，保护 `core/systems/state/render/audio/ui/events/fx` 的依赖方向和非 host 模块不访问 `window/document/localStorage/sessionStorage`。新增 `docs/testing/browser-smoke.md`，覆盖 boot、单人、双人、出钩、暂停/恢复、商店购买和固定 seed 文本快照。为适配 ES module 入口，`run.command`、`start.sh`、`start.bat` 改为启动本地静态服务器后打开页面，README 同步说明不要依赖直接 `file://`。自动审查初审 FAIL：shop smoke 不可重复、暂停快捷键写成 Escape；已新增 `src/testing/debugApi.js:createDebugShopSetup()`、`window.__goldMinerSmoke.enterShop({ score })` 和 debug snapshot `inventory` 字段，使商店 smoke 可固定 `score=500` 后购买第一项并断言 `score=350` / `inventory.bombs=1`；文档改为 `P` 或暂停按钮。验证：`node --test tests/unit/debug-api.test.mjs tests/unit/runtime-bridge.test.mjs tests/unit/source-invariants.test.mjs tests/unit/dependency-boundaries.test.mjs` 通过 48/48；`npm run check:syntax` 通过；`zsh -n run.command && sh -n start.sh` 通过；code reviewer 复审 PASS。
- 架构 Task 20 全量 Node 验证：扩展后的 `npm run verify` 通过 246/246。
- 架构 Task 21 final validation/docs：`docs/architecture-optimization-plan.md` 增加 2026-05-25 当前执行状态和刻意延期项，`docs/superpowers/plans/2026-05-24-architecture-next-batches.md` 更新 Batch 7/8 为本轮已完成范围。最终验证：`npm run verify` 通过 246/246；`./macos/build.command` 成功生成 `/Users/drew/Project/Gold Miner/dist/macos/黄金矿工.app`；浏览器 smoke 使用 `http://127.0.0.1:5173/?seed=12345`，确认 `window.__goldMinerModulesReady === true`、无 `__goldMinerBootError`、无 `__goldMiner*Error`、canvas 采样 3772/3772 非空、单人局进入 playing、`window.advanceTime(1000)` 推进时钟且 seed 保持、Space 出钩后 hook 从 swing 进入 retract、`P` 暂停/恢复、`window.__goldMinerSmoke.enterShop({ score: 500 })` 后购买第一项得到 `score=350` 且 `inventory.bombs=1`、双人局 hooks=2 且 Space/Enter 两名玩家均可出钩。控制台 error 为 0；warning 仅为 smoke 脚本多次 `getImageData()` 采样触发的 Canvas readback 性能提示。最终 canvas 截图见 `output/gold-miner-final-smoke.png`。
- 视觉重构：新增并接入 `assets/art/crayon/` 写实素描纸蜡笔资产包，背景、木梁、矿物、老鼠、矿工、绞盘、钩爪、粒子、道具图标与 UI 均改为纸面蜡笔/炭笔视觉；玩法状态、尺寸、碰撞、关卡、商店和输入逻辑保持不变。最终修正让矿工/绞盘/钩爪在资产已加载时优先使用贴图分支，过程绘制只作兜底；UI 改为 flex 页面壳以适配移动端换行头部，并提高商店库存小字对比度。验证：`npm run verify` 通过 264/264；Browser smoke 在 `http://127.0.0.1:5173/?seed=12345` 确认 crayon 资产 27/27 加载、单人 Space 出钩、`P` 暂停/恢复、商店固定 500 分购买后 `score=350` 且 `inventory.bombs=1`、双人 Space/Enter 两条钩索均进入 extend、移动端 `390x720` 下 stage 从真实 topbar 高度后开始且 `bodyScrollHeight=720`。最终 gameplay canvas 截图见 `output/gold-miner-crayon-final.png`。
- 视觉重构缓存修复：`index.html` 的 stylesheet 入口改为 `styles.css?v=crayon-paper-20260526`，避免浏览器继续使用旧深色 UI 缓存；source invariant 增加版本化 stylesheet 断言。Browser 验证模式选择界面、顶部栏和按钮均加载 `ui-paper.png` 纸面纹理，截图见 `output/gold-miner-crayon-ui.png`；`npm run verify` 通过 264/264。
- 图标设计：重做 `assets/icons/` 和 `macos/AppIcon.*` 为写实素描纸蜡笔风格，主体为矿洞木梁、钩爪和金块，并补齐 1024/512/192/180/32/16 PNG、multi-size favicon.ico 与 macOS icns；`index.html` 图标入口增加 `crayon-icon-20260526` 缓存版本，预览见 `output/gold-miner-app-icon-preview.png`。
